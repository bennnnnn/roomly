/**
 * Placeholder so the admin package has at least one source file that
 * typechecks and lints cleanly. Real Next.js App Router routes land in Slice 7.
 */

import { COLORS } from '@roomly/ui-tokens';

export const ADMIN_ACCENT_HEX = COLORS.accent[500];
