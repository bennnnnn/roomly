{
  "name": "@roomly/mobile",
  "version": "0.0.0",
  "private": true,
  "description": "Roomly mobile app (Expo SDK 56 + React Native + Expo Router). Scaffolded in Slice 1.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "echo 'no tests yet — Slice 1 adds them' && exit 0"
  },
  "dependencies": {
    "@roomly/lib": "workspace:*",
    "@roomly/ui-tokens": "workspace:*"
  }
}
