{
  "name": "@roomly/ui-tokens",
  "version": "0.0.0",
  "private": true,
  "description": "Design tokens (colors, spacing, radii, fonts) shared between mobile (NativeWind) and admin (Tailwind v3). See ADR-0001.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tailwind": "./src/tailwind.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "echo 'no tests in ui-tokens' && exit 0"
  }
}
